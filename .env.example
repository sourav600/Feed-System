# Copy to .env on the deploy server (never commit the real .env).
# Generate both values with: openssl rand -base64 32

# Password for the `newsfeed` Postgres role (see README.md section 1).
DB_PASSWORD=

# HS256 signing secret for access/refresh JWTs. Must be >= 256 bits.
JWT_SECRET=
